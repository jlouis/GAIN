* Get rebar config and a hook to the erlang-riak-client
* Write a simple reader for the file on disk
* Simplify keys used by riak
* Store affinity data into riak

* Think about how to write M/R for KMeans in M/R-Erlang.

